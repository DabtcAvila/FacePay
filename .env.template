# FacePay Environment Variables Template
# Copy this file to .env.local and update with your actual values

# ============================================================================
# DATABASE CONFIGURATION
# ============================================================================
# PostgreSQL connection string from Supabase or your database provider
DATABASE_URL="postgresql://postgres:password@db.xxx.supabase.co:5432/postgres"
DIRECT_URL="postgresql://postgres:password@db.xxx.supabase.co:5432/postgres"

# ============================================================================
# AUTHENTICATION & SECURITY
# ============================================================================
# Generate a secure random string (64+ characters)
# You can use: openssl rand -base64 64
JWT_SECRET="your-super-secure-jwt-secret-64-characters-minimum-here-please-make-it-random"

# Generate with: openssl rand -base64 32
NEXTAUTH_SECRET="your-nextauth-secret-32-chars-minimum"

# Your application URL (update for production)
NEXTAUTH_URL="http://localhost:3000"
NEXT_PUBLIC_APP_URL="http://localhost:3000"

# ============================================================================
# WEBAUTHN CONFIGURATION
# ============================================================================
WEBAUTHN_RP_NAME="FacePay"
WEBAUTHN_RP_ID="localhost"  # Change to your domain for production
WEBAUTHN_ORIGIN="http://localhost:3000"  # Change to https://yourdomain.com for production

# ============================================================================
# PAYMENT PROVIDERS (Optional)
# ============================================================================
# Stripe (create account at stripe.com)
STRIPE_SECRET_KEY="sk_test_..."
STRIPE_PUBLISHABLE_KEY="pk_test_..."
STRIPE_WEBHOOK_SECRET="whsec_..."

# MercadoPago (for Latin America - create account at mercadopago.com)
MERCADOPAGO_ACCESS_TOKEN="your-mp-access-token"
MERCADOPAGO_PUBLIC_KEY="your-mp-public-key"

# ============================================================================
# MONITORING & ANALYTICS (Optional)
# ============================================================================
# Sentry (error tracking - free tier: 5K errors/month)
NEXT_PUBLIC_SENTRY_DSN="https://your-sentry-dsn@sentry.io/project-id"

# Mixpanel (analytics - free tier: 20M events/month)
NEXT_PUBLIC_MIXPANEL_TOKEN="your-mixpanel-token"

# Google Analytics (optional)
NEXT_PUBLIC_GA_ID="G-XXXXXXXXXX"

# ============================================================================
# EMAIL SERVICE (Optional)
# ============================================================================
# For transactional emails (receipts, notifications)
EMAIL_FROM="noreply@yourdomain.com"
EMAIL_PROVIDER="smtp"  # smtp, sendgrid, mailgun, ses

# SMTP Configuration
SMTP_HOST="smtp.gmail.com"
SMTP_PORT="587"
SMTP_USER="your-email@gmail.com"
SMTP_PASS="your-app-password"

# SendGrid (alternative)
SENDGRID_API_KEY="SG.xxx"

# ============================================================================
# BLOCKCHAIN/CRYPTO (Optional)
# ============================================================================
# For cryptocurrency payments
ETHEREUM_RPC_URL="https://mainnet.infura.io/v3/your-project-id"
POLYGON_RPC_URL="https://polygon-rpc.com"

# ============================================================================
# RATE LIMITING & SECURITY (Optional)
# ============================================================================
# Rate limiting configuration
RATE_LIMIT_WINDOW="15"  # minutes
RATE_LIMIT_MAX="100"    # requests per window

# Security settings
CORS_ORIGIN="http://localhost:3000"  # Update for production
ALLOWED_HOSTS="localhost,yourdomain.com"

# ============================================================================
# DEVELOPMENT SETTINGS
# ============================================================================
# Environment
NODE_ENV="development"  # production, development, test

# Debug settings
DEBUG="false"
LOG_LEVEL="info"  # error, warn, info, debug

# ============================================================================
# PRODUCTION OVERRIDES
# ============================================================================
# When deploying to production, update these:
#
# NEXTAUTH_URL="https://yourdomain.com"
# NEXT_PUBLIC_APP_URL="https://yourdomain.com"
# WEBAUTHN_RP_ID="yourdomain.com"
# WEBAUTHN_ORIGIN="https://yourdomain.com"
# NODE_ENV="production"
# DEBUG="false"
# CORS_ORIGIN="https://yourdomain.com"

# ============================================================================
# INSTRUCTIONS
# ============================================================================
#
# 1. Copy this file to .env.local:
#    cp .env.template .env.local
#
# 2. Update DATABASE_URL with your PostgreSQL connection string
#    - For Supabase: Get from Project Settings > Database
#    - Format: postgresql://postgres:password@host:5432/database
#
# 3. Generate secure secrets:
#    - JWT_SECRET: openssl rand -base64 64
#    - NEXTAUTH_SECRET: openssl rand -base64 32
#
# 4. For production deployment:
#    - Update all localhost URLs to your domain
#    - Use HTTPS instead of HTTP
#    - Set NODE_ENV=production
#
# 5. Optional services:
#    - Set up Stripe for payments
#    - Configure Sentry for error tracking
#    - Add Mixpanel for analytics
#
# 6. Never commit .env.local to version control!
#    - Add .env.local to .gitignore
#    - Use your platform's environment variable settings for production
#
# For detailed setup instructions, see:
# - DEPLOYMENT_GUIDE.md
# - scripts/deployment-checklist.md