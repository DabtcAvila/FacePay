// Prisma optimized client placeholder
import { prisma } from '@/lib/prisma';

// Export the regular prisma client until optimized extensions are configured
export const prismaOptimized = prisma;
export default prismaOptimized;